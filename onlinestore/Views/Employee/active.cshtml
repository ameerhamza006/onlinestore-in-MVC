@model onlinestore.Models.login
@{
    ViewBag.Title = "active";
    Layout = "~/Views/shared/backtheme.cshtml";
}


<div class="col-8 mt-5 offset-2">
    <div class="card">
        <div class="card-body">

            @using (Html.BeginForm("active", "Employee", FormMethod.Post))
            {
                <div class="form-group offset-3">
                   
                    <div class="col-md-6 btn btn-dark btn-lg btn-block">
                        @Html.EditorFor(a => a.log_status)

                    </div>
                </div>

                <div class="form-group">
                    <div class="offset-4 col-md-2">
                        <button type="submit" class="btn btn-secondary btn-lg btn-block"> Apply </button>

                    </div>
                </div>

            }









        </div>
                </div>
            </div>
